'use strict';

const Validator = require("../validator");

let str = 'yes';
let num = 1;
let arr = ['a'];
let obj = {x:'y'};
let func = () => {};
let bool = false;

const schema = {
  fields
}

describe('Validator js validates', () => {

  test('hello', () => {
    expect()
  });

  }
})
