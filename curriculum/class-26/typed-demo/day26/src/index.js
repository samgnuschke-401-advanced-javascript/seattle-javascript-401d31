import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App/App'

// Vinicio - This is the link between the virtual DOM and the
// real DOM
ReactDOM.render(<App/>, document.getElementById('root'));
